> r"""
> 天机组合式Skill编排引擎 (Tianji Workflow Engine) v1.1
> =====================================================
> 借鉴Hermes Agent的resolve_toolset()组合式工具集，
> 为元初系统v6.0实现Workflow as Code的完整编排能力。
  
> v1.1: M10 升级 — execute()喂入record_action + recorder/learning_engine集成
  
> Hermes组合模式:
>   Toolset = [tool_a, tool_b, includes: [toolset_c]]
>   resolve_toolset() → 递归展开 → 扁平化工具列表
  
> 天机增强:
>   Workflow = Stage[Skill+Agent] → Stage[Skill+Agent] → ...
>   + 条件分支: 根据上一阶段结果决定下一阶段
>   + 并行阶段: 多个Skill同时执行
>   + 错误恢复: 失败阶段自动重试/跳过/降级
>   + TVP声明: 每次Agent切换自动TVP声明
>   + 天机归档: 工作流执行记录自动归档到L3+L4
  
> 架构位置: 天机/core/workflow_engine.py
> 依赖: 天机/core/skill_registry.py, 天机/core/async_bridge.py
> """
  
> import json
> import time
> import uuid
> import threading
> import logging
> from pathlib import Path
> from typing import Any, Optional, Dict, List, Callable, Tuple, Set
> from dataclasses import dataclass, field
> from datetime import datetime
> from enum import Enum
  
> logger = logging.getLogger("tianji.workflow")
  
  
> class StageStatus(str, Enum):
>     PENDING = "pending"
>     RUNNING = "running"
>     SUCCESS = "success"
>     FAILED = "failed"
>     SKIPPED = "skipped"
>     RETRYING = "retrying"
  
  
> class WorkflowStatus(str, Enum):
>     PENDING = "pending"
>     RUNNING = "running"
>     COMPLETED = "completed"
>     FAILED = "failed"
>     PARTIAL = "partial"
>     CANCELLED = "cancelled"
  
  
> class StageType(str, Enum):
>     SEQUENTIAL = "sequential"
>     PARALLEL = "parallel"
>     CONDITIONAL = "conditional"
  
  
> @dataclass
> class StageResult:
>     stage_name: str
>     status: StageStatus
>     agent_id: str
>     skill_name: str
>     output: Optional[Dict] = None
>     error: str = ""
>     duration_ms: float = 0.0
>     memory_ids: List[str] = field(default_factory=list)
>     tvp_declared: bool = False
  
>     def to_dict(self) -> dict:
!         return {
!             "stage_name": self.stage_name,
!             "status": self.status.value,
!             "agent_id": self.agent_id,
!             "skill_name": self.skill_name,
!             "duration_ms": self.duration_ms,
!             "error": self.error,
!             "memory_ids": self.memory_ids,
!             "tvp_declared": self.tvp_declared,
!         }
  
  
> @dataclass
> class WorkflowStage:
>     name: str
>     skill_name: str
>     agent_id: str
>     stage_type: StageType = StageType.SEQUENTIAL
>     condition: Optional[str] = None
>     retry_count: int = 0
>     max_retries: int = 2
>     timeout_ms: float = 60000.0
>     fallback_agent: Optional[str] = None
>     fallback_skill: Optional[str] = None
>     required_input_keys: List[str] = field(default_factory=list)
>     output_key: Optional[str] = None
>     parallel_group: Optional[str] = None
  
>     def to_dict(self) -> dict:
!         return {
!             "name": self.name,
!             "skill_name": self.skill_name,
!             "agent_id": self.agent_id,
!             "stage_type": self.stage_type.value,
!             "condition": self.condition,
!             "max_retries": self.max_retries,
!             "timeout_ms": self.timeout_ms,
!             "fallback_agent": self.fallback_agent,
!             "parallel_group": self.parallel_group,
!         }
  
  
> @dataclass
> class WorkflowDefinition:
>     name: str
>     description: str
>     stages: List[WorkflowStage]
>     tags: List[str] = field(default_factory=list)
>     version: str = "1.0.0"
>     composition_name: Optional[str] = None
  
>     def to_dict(self) -> dict:
!         return {
!             "name": self.name,
!             "description": self.description,
!             "stages": [s.to_dict() for s in self.stages],
!             "tags": self.tags,
!             "version": self.version,
!             "composition_name": self.composition_name,
!         }
  
  
> @dataclass
> class WorkflowExecution:
>     execution_id: str
>     workflow_name: str
>     status: WorkflowStatus
>     stages: List[StageResult] = field(default_factory=list)
>     context: Dict[str, Any] = field(default_factory=dict)
>     started_at: float = 0.0
>     completed_at: float = 0.0
>     total_duration_ms: float = 0.0
>     memory_ids: List[str] = field(default_factory=list)
  
>     def to_dict(self) -> dict:
!         return {
!             "execution_id": self.execution_id,
!             "workflow_name": self.workflow_name,
!             "status": self.status.value,
!             "stages": [s.to_dict() for s in self.stages],
!             "total_duration_ms": self.total_duration_ms,
!             "memory_ids": self.memory_ids,
!         }
  
  
> BUILTIN_WORKFLOWS: Dict[str, Dict] = {
>     "novel-creation-pipeline": {
>         "name": "novel-creation-pipeline",
>         "description": "小说创作全流程: 世界观→章节→审校→导出",
>         "stages": [
>             {
>                 "name": "worldbuilding",
>                 "skill_name": "novel-worldbuilding-expand",
>                 "agent_id": "miaobi",
>                 "output_key": "worldview",
>             },
>             {
>                 "name": "chapter_creation",
>                 "skill_name": "novel-chapter-create",
>                 "agent_id": "miaobi",
>                 "required_input_keys": ["worldview"],
>                 "output_key": "chapter",
>             },
>             {
>                 "name": "consistency_check",
>                 "skill_name": "novel-consistency-check",
>                 "agent_id": "mingjing",
>                 "required_input_keys": ["chapter"],
>                 "output_key": "review",
>             },
>             {
>                 "name": "conditional_revision",
>                 "skill_name": "novel-chapter-create",
>                 "agent_id": "miaobi",
>                 "stage_type": "conditional",
>                 "condition": "review.has_issues",
>                 "required_input_keys": ["review", "chapter"],
>                 "output_key": "revised_chapter",
>                 "fallback_agent": "miaobi",
>                 "fallback_skill": "novel-chapter-create",
>             },
>             {
>                 "name": "format_export",
>                 "skill_name": "novel-format-export",
>                 "agent_id": "jinshu",
>                 "required_input_keys": ["chapter"],
>                 "output_key": "export_path",
>             },
>             {
>                 "name": "version_track",
>                 "skill_name": "novel-version-track",
>                 "agent_id": "shiguan",
>                 "required_input_keys": ["export_path"],
>                 "output_key": "version_id",
>             },
>         ],
>         "tags": ["novel", "creation", "pipeline"],
>     },
>     "memory-lifecycle": {
>         "name": "memory-lifecycle",
>         "description": "记忆全生命周期: 捕获→存储→检索→巩固",
>         "stages": [
>             {
>                 "name": "capture",
>                 "skill_name": "auto-memory-capture",
>                 "agent_id": "dongcha",
>                 "output_key": "raw_data",
>             },
>             {
>                 "name": "store",
>                 "skill_name": "memory-remember",
>                 "agent_id": "yiku",
>                 "required_input_keys": ["raw_data"],
>                 "output_key": "memory_id",
>             },
>             {
>                 "name": "retrieve",
>                 "skill_name": "memory-recall",
>                 "agent_id": "yiku",
>                 "required_input_keys": ["memory_id"],
>                 "output_key": "recalled",
>             },
>         ],
>         "tags": ["memory", "lifecycle", "core"],
>     },
>     "corpus-pipeline": {
>         "name": "corpus-pipeline",
>         "description": "语料处理全流程: 提取→评分→检索→批量导入",
>         "stages": [
>             {
>                 "name": "extract",
>                 "skill_name": "corpus-extract",
>                 "agent_id": "kuangshi",
>                 "output_key": "raw_corpus",
>             },
>             {
>                 "name": "quality_score",
>                 "skill_name": "corpus-quality-score",
>                 "agent_id": "tiansuan",
>                 "required_input_keys": ["raw_corpus"],
>                 "output_key": "scored_corpus",
>             },
>             {
>                 "name": "retrieve_and_import",
>                 "skill_name": "corpus-batch-import",
>                 "agent_id": "kuangshi",
>                 "required_input_keys": ["scored_corpus"],
>                 "output_key": "import_result",
>                 "stage_type": "parallel",
>                 "parallel_group": "import",
>             },
>         ],
>         "tags": ["corpus", "pipeline"],
>     },
>     "system-health-check": {
>         "name": "system-health-check",
>         "description": "系统健康检查: 诊断→记忆→修复",
>         "stages": [
>             {
>                 "name": "diagnose",
>                 "skill_name": "system-diagnose",
>                 "agent_id": "qianli",
>                 "output_key": "diagnosis",
>             },
>             {
>                 "name": "recall_history",
>                 "skill_name": "memory-recall",
>                 "agent_id": "yiku",
>                 "required_input_keys": ["diagnosis"],
>                 "output_key": "history",
>             },
>             {
>                 "name": "repair",
>                 "skill_name": "memory-remember",
>                 "agent_id": "yiku",
>                 "stage_type": "conditional",
>                 "condition": "diagnosis.needs_repair",
>                 "required_input_keys": ["diagnosis", "history"],
>                 "output_key": "repair_result",
>             },
>         ],
>         "tags": ["system", "health", "diagnostics"],
>     },
> }
  
  
> class WorkflowEngine:
>     """
>     组合式Skill编排引擎 — 借鉴Hermes的resolve_toolset()
  
>     Hermes: Toolset = [tool_a, tool_b, includes: [toolset_c]]
>     天机:   Workflow = Stage[Skill+Agent] → Stage[Skill+Agent] → ...
  
>     核心能力:
>       1. execute() — 执行工作流，支持顺序/并行/条件分支
>       2. define() — 从Composition自动生成Workflow
>       3. resume() — 从失败阶段恢复执行
>       4. cancel() — 取消正在执行的工作流
>     """
  
>     def __init__(self, skill_registry=None, async_bridge=None,
>                  event_bus=None, memory_api_url: str = "http://127.0.0.1:8771",
>                  recorder: Optional[Any] = None,
>                  learning_engine: Optional[Any] = None):
!         self._skill_registry = skill_registry
!         self._bridge = async_bridge
!         self._event_bus = event_bus
!         self._memory_api_url = memory_api_url
!         self._recorder = recorder
!         self._learning_engine = learning_engine
!         self._workflows: Dict[str, WorkflowDefinition] = {}
!         self._executions: Dict[str, WorkflowExecution] = {}
!         self._lock = threading.Lock()
!         self._stats = {
!             "total_executions": 0,
!             "completed_executions": 0,
!             "failed_executions": 0,
!             "partial_executions": 0,
!             "total_stages_executed": 0,
!             "total_tvp_declarations": 0,
!             "avg_workflow_duration_ms": 0.0,
!             "errors": 0,
!         }
  
!         self._evo_loop = None
!         try:
!             from .evolution_loop import EvolutionLoop
!             self._evo_loop = EvolutionLoop(
!                 module_name="workflow_engine",
!                 effectiveness_fn=self._calc_workflow_effectiveness,
!                 learn_fn=self._learn_from_workflows,
!                 evolve_fn=self._evolve_workflow_config,
!                 mutable_config={
!                     "default_timeout_ms": 60000,
!                     "max_retries": 2,
!                     "fallback_enabled": True,
!                     "parallel_max_concurrent": 3,
!                 },
!                 health_metrics_fn=self._get_workflow_health,
!                 recorder=recorder,
!             )
!         except ImportError:
!             pass
  
!         self._load_builtin_workflows()
  
>     def _calc_workflow_effectiveness(self, action: str, state_before: Dict, state_after: Dict) -> float:
!         if action == "workflow_execute":
!             status = state_after.get("status", "")
!             if status == "completed":
!                 return 0.5
!             if status == "partial":
!                 return -0.2
!             if status == "failed":
!                 return -0.5
!         if action == "stage_execute":
!             if state_after.get("success", False):
!                 return 0.3
!             if state_after.get("retried", False):
!                 return -0.1
!             return -0.3
!         return 0.0
  
>     def _learn_from_workflows(self, causal_pairs, effectiveness_summary) -> Dict:
!         failed = sum(1 for p in causal_pairs if p.action == "workflow_execute" and p.effectiveness < 0)
!         stage_fails = sum(1 for p in causal_pairs if p.action == "stage_execute" and p.effectiveness < 0)
!         timeouts = sum(1 for p in causal_pairs if p.action == "stage_execute" and p.effectiveness == -0.1)
!         return {
!             "failed_workflows": failed,
!             "stage_failures": stage_fails,
!             "timeouts": timeouts,
!             "avg_effectiveness": effectiveness_summary.get("avg", 0.0),
!         }
  
>     def _evolve_workflow_config(self, learn_result, mutable_config) -> Dict:
!         changes = []
!         if learn_result.get("timeouts", 0) > 5:
!             old_timeout = mutable_config.get("default_timeout_ms", 60000)
!             new_timeout = min(int(old_timeout * 1.3), 300000)
!             changes.append({"rule": "default_timeout_ms", "old_value": old_timeout, "new_value": new_timeout})
!         if learn_result.get("stage_failures", 0) > 8:
!             old_retries = mutable_config.get("max_retries", 2)
!             new_retries = min(old_retries + 1, 5)
!             changes.append({"rule": "max_retries", "old_value": old_retries, "new_value": new_retries})
!         return {"changes": changes}
  
>     def _get_workflow_health(self) -> Dict[str, float]:
!         total = max(self._stats.get("total_executions", 1), 1)
!         return {
!             "success_rate": self._stats.get("completed_executions", 0) / total,
!             "error_rate": self._stats.get("errors", 0) / total,
!         }
  
>     @property
>     def evolution_loop(self):
!         return self._evo_loop
  
>     @property
>     def recorder(self):
!         return self._recorder
  
>     @property
>     def learning_engine(self):
!         return self._learning_engine
  
>     def _load_builtin_workflows(self):
!         for wf_name, wf_data in BUILTIN_WORKFLOWS.items():
!             stages = []
!             for s_data in wf_data["stages"]:
!                 stage = WorkflowStage(
!                     name=s_data["name"],
!                     skill_name=s_data["skill_name"],
!                     agent_id=s_data["agent_id"],
!                     stage_type=StageType(s_data.get("stage_type", "sequential")),
!                     condition=s_data.get("condition"),
!                     max_retries=s_data.get("max_retries", 2),
!                     timeout_ms=s_data.get("timeout_ms", 60000.0),
!                     fallback_agent=s_data.get("fallback_agent"),
!                     fallback_skill=s_data.get("fallback_skill"),
!                     required_input_keys=s_data.get("required_input_keys", []),
!                     output_key=s_data.get("output_key"),
!                     parallel_group=s_data.get("parallel_group"),
!                 )
!                 stages.append(stage)
  
!             wf = WorkflowDefinition(
!                 name=wf_data["name"],
!                 description=wf_data["description"],
!                 stages=stages,
!                 tags=wf_data.get("tags", []),
!                 composition_name=wf_name,
!             )
!             self._workflows[wf_name] = wf
  
>     def register_workflow(self, definition: WorkflowDefinition) -> bool:
!         with self._lock:
!             self._workflows[definition.name] = definition
  
!         if self._evo_loop:
!             try:
!                 self._evo_loop.record_action(
!                     action="workflow_register",
!                     state_before={"count": len(self._workflows) - 1},
!                     state_after={"count": len(self._workflows), "name": definition.name},
!                     metadata={"workflow": definition.name, "stages": len(definition.stages)},
!                 )
!             except Exception:
!                 pass
  
!         logger.info(f"[WorkflowEngine] Registered workflow: {definition.name} "
!                      f"({len(definition.stages)} stages)")
!         return True
  
>     def define_from_composition(self, composition_name: str) -> Optional[WorkflowDefinition]:
>         """
>         从SkillComposition自动生成Workflow — 天机独有
  
>         将Composition中的Skill列表自动编排为顺序Workflow，
>         每个Skill根据required_agents自动分配Agent。
>         """
!         if not self._skill_registry:
!             return None
  
!         comp = self._skill_registry.get_composition(composition_name)
!         if not comp:
!             return None
  
!         skills = self._skill_registry.resolve_composition(composition_name)
!         if not skills:
!             return None
  
!         stages = []
!         for i, skill in enumerate(skills):
!             agent_id = skill.required_agents[0] if skill.required_agents else "baiqiao"
!             stage = WorkflowStage(
!                 name=f"stage_{i+1}_{skill.name}",
!                 skill_name=skill.name,
!                 agent_id=agent_id,
!                 output_key=f"stage_{i+1}_output",
!             )
!             if i > 0:
!                 stage.required_input_keys = [f"stage_{i}_output"]
!             stages.append(stage)
  
!         wf = WorkflowDefinition(
!             name=composition_name,
!             description=comp.description,
!             stages=stages,
!             tags=comp.tags,
!             composition_name=composition_name,
!         )
!         self.register_workflow(wf)
!         return wf
  
>     def execute(self, workflow_name: str,
>                 context: Optional[Dict[str, Any]] = None,
>                 resume_from: Optional[str] = None) -> WorkflowExecution:
>         """
>         执行工作流 — 核心编排逻辑
  
>         支持三种阶段类型:
>           1. SEQUENTIAL — 顺序执行，上一阶段输出作为下一阶段输入
>           2. PARALLEL   — 并行执行，同parallel_group的阶段同时运行
>           3. CONDITIONAL — 条件执行，根据condition表达式决定是否执行
>         """
!         wf = self._workflows.get(workflow_name)
!         if not wf:
!             return WorkflowExecution(
!                 execution_id="",
!                 workflow_name=workflow_name,
!                 status=WorkflowStatus.FAILED,
!             )
  
!         execution = WorkflowExecution(
!             execution_id=str(uuid.uuid4())[:12],
!             workflow_name=workflow_name,
!             status=WorkflowStatus.RUNNING,
!             context=context or {},
!             started_at=time.time(),
!         )
  
!         self._stats["total_executions"] += 1
  
!         start_idx = 0
!         if resume_from:
!             for i, stage in enumerate(wf.stages):
!                 if stage.name == resume_from:
!                     start_idx = i
!                     break
  
!         parallel_groups: Dict[str, List[Tuple[int, WorkflowStage]]] = {}
!         for i, stage in enumerate(wf.stages):
!             if i < start_idx:
!                 continue
!             if stage.stage_type == StageType.PARALLEL and stage.parallel_group:
!                 parallel_groups.setdefault(stage.parallel_group, []).append((i, stage))
  
!         executed_parallel_groups: Set[str] = set()
  
!         for i, stage in enumerate(wf.stages):
!             if i < start_idx:
!                 continue
  
!             if stage.stage_type == StageType.PARALLEL and stage.parallel_group:
!                 group_name = stage.parallel_group
!                 if group_name in executed_parallel_groups:
!                     continue
!                 executed_parallel_groups.add(group_name)
  
!                 group_stages = parallel_groups.get(group_name, [])
!                 group_results = self._execute_parallel_stages(
!                     group_stages, execution.context, wf.name, execution.execution_id
!                 )
!                 for stage_result in group_results:
!                     execution.stages.append(stage_result)
!                     if stage_result.status == StageStatus.SUCCESS and stage.output_key:
!                         execution.context[stage.output_key] = stage_result.output
!                     self._stats["total_stages_executed"] += 1
!                     self._stats["total_tvp_declarations"] += 1 if stage_result.tvp_declared else 0
!                 continue
  
!             stage_result = self._execute_stage(stage, execution.context, wf.name, execution.execution_id)
!             execution.stages.append(stage_result)
!             self._stats["total_stages_executed"] += 1
  
!             if stage_result.status == StageStatus.SUCCESS:
!                 if stage.output_key:
!                     execution.context[stage.output_key] = stage_result.output
!             elif stage_result.status == StageStatus.FAILED:
!                 if stage.fallback_agent:
!                     fallback_result = self._execute_fallback(stage, execution.context, wf.name)
!                     execution.stages.append(fallback_result)
!                     if fallback_result.status == StageStatus.SUCCESS and stage.output_key:
!                         execution.context[stage.output_key] = fallback_result.output
!                 elif stage.max_retries > 0 and stage_result.retry_count < stage.max_retries:
!                     retry_result = self._retry_stage(stage, execution.context, wf.name)
!                     execution.stages.append(retry_result)
!                     if retry_result.status == StageStatus.SUCCESS and stage.output_key:
!                         execution.context[stage.output_key] = retry_result.output
!                     else:
!                         execution.status = WorkflowStatus.PARTIAL
!                         self._stats["partial_executions"] += 1
!                         break
!                 else:
!                     execution.status = WorkflowStatus.PARTIAL
!                     self._stats["partial_executions"] += 1
!                     break
  
!             self._stats["total_tvp_declarations"] += 1 if stage_result.tvp_declared else 0
  
!         if execution.status == WorkflowStatus.RUNNING:
!             execution.status = WorkflowStatus.COMPLETED
!             self._stats["completed_executions"] += 1
  
!         execution.completed_at = time.time()
!         execution.total_duration_ms = (execution.completed_at - execution.started_at) * 1000
  
!         self._archive_execution(execution)
  
!         if self._evo_loop:
!             try:
!                 self._evo_loop.record_action(
!                     action="workflow_execute",
!                     state_before={"status": "pending", "stages": len(wf.stages)},
!                     state_after={"status": execution.status.value, "completed": len(execution.stages)},
!                     metadata={"workflow": workflow_name, "execution_id": execution.execution_id},
!                 )
!             except Exception:
!                 pass
  
!         with self._lock:
!             self._executions[execution.execution_id] = execution
  
!         return execution
  
>     def _execute_stage(self, stage: WorkflowStage, context: Dict,
>                        workflow_name: str, execution_id: str) -> StageResult:
>         """执行单个阶段 — 含TVP声明"""
!         start = time.monotonic()
!         self._declare_tvp(stage, workflow_name, execution_id)
  
!         result = StageResult(
!             stage_name=stage.name,
!             status=StageStatus.RUNNING,
!             agent_id=stage.agent_id,
!             skill_name=stage.skill_name,
!             tvp_declared=True,
!         )
  
!         if stage.stage_type == StageType.CONDITIONAL and stage.condition:
!             if not self._evaluate_condition(stage.condition, context):
!                 result.status = StageStatus.SKIPPED
!                 result.duration_ms = (time.monotonic() - start) * 1000
!                 return result
  
!         try:
!             stage_output = self._invoke_skill(stage, context)
!             result.status = StageStatus.SUCCESS
!             result.output = stage_output
!             result.duration_ms = (time.monotonic() - start) * 1000
!         except Exception as e:
!             result.status = StageStatus.FAILED
!             result.error = str(e)
!             result.duration_ms = (time.monotonic() - start) * 1000
!             logger.error(f"Stage {stage.name} failed: {e}")
  
!         return result
  
>     def _execute_parallel_stages(self, group_stages: List[Tuple[int, WorkflowStage]],
>                                   context: Dict, workflow_name: str,
>                                   execution_id: str) -> List[StageResult]:
>         """并行执行同组阶段"""
!         from concurrent.futures import ThreadPoolExecutor, as_completed
  
!         results = []
!         with ThreadPoolExecutor(max_workers=len(group_stages),
!                                  thread_name_prefix="wf-parallel") as pool:
!             futures = {}
!             for _, stage in group_stages:
!                 future = pool.submit(
!                     self._execute_stage, stage, context, workflow_name, execution_id
!                 )
!                 futures[future] = stage
  
!             for future in as_completed(futures, timeout=120):
!                 try:
!                     result = future.result(timeout=60)
!                     results.append(result)
!                 except Exception as e:
!                     stage = futures[future]
!                     results.append(StageResult(
!                         stage_name=stage.name,
!                         status=StageStatus.FAILED,
!                         agent_id=stage.agent_id,
!                         skill_name=stage.skill_name,
!                         error=str(e),
!                     ))
  
!         return results
  
>     def _execute_fallback(self, stage: WorkflowStage, context: Dict,
>                            workflow_name: str) -> StageResult:
>         """执行降级方案"""
!         if not stage.fallback_agent or not stage.fallback_skill:
!             return StageResult(
!                 stage_name=f"{stage.name}_fallback",
!                 status=StageStatus.FAILED,
!                 agent_id=stage.agent_id,
!                 skill_name=stage.skill_name,
!                 error="No fallback configured",
!             )
  
!         fallback_stage = WorkflowStage(
!             name=f"{stage.name}_fallback",
!             skill_name=stage.fallback_skill,
!             agent_id=stage.fallback_agent,
!             required_input_keys=stage.required_input_keys,
!             output_key=stage.output_key,
!         )
!         return self._execute_stage(fallback_stage, context, workflow_name, "")
  
>     def _retry_stage(self, stage: WorkflowStage, context: Dict,
>                       workflow_name: str) -> StageResult:
>         """重试失败阶段"""
!         result = self._execute_stage(stage, context, workflow_name, "")
!         result.status = StageStatus.RETRYING
!         return result
  
>     def _invoke_skill(self, stage: WorkflowStage, context: Dict) -> Optional[Dict]:
>         """
>         调用Skill — 实际执行逻辑
  
>         当前实现: 记录到天机 + 返回模拟结果
>         未来: 集成Trae IDE的Skill执行API
>         """
!         skill_input = {}
!         for key in stage.required_input_keys:
!             if key in context:
!                 skill_input[key] = context[key]
  
!         if self._bridge:
!             bridge_result = self._bridge.memory_post(
!                 content=json.dumps({
!                     "type": "workflow_stage_invocation",
!                     "workflow": stage.name,
!                     "skill": stage.skill_name,
!                     "agent": stage.agent_id,
!                     "input_keys": list(skill_input.keys()),
!                 }, ensure_ascii=False),
!                 layer="working",
!                 tags=["workflow", stage.skill_name, stage.agent_id, "invocation"],
!                 priority="medium",
!             )
!             return {
!                 "skill_name": stage.skill_name,
!                 "agent_id": stage.agent_id,
!                 "status": "invoked",
!                 "memory_id": bridge_result.memory_id if bridge_result.success else "",
!             }
  
!         return {
!             "skill_name": stage.skill_name,
!             "agent_id": stage.agent_id,
!             "status": "invoked_no_bridge",
!         }
  
>     def _evaluate_condition(self, condition: str, context: Dict) -> bool:
>         """评估条件表达式"""
!         try:
!             parts = condition.split(".")
!             obj = context
!             for part in parts:
!                 if isinstance(obj, dict):
!                     obj = obj.get(part)
!                 else:
!                     return False
!             return bool(obj)
!         except Exception:
!             return False
  
>     def _declare_tvp(self, stage: WorkflowStage, workflow_name: str,
>                       execution_id: str):
>         """TVP透明调度声明 — 每次Agent切换必须声明"""
!         tvp_msg = (
!             f"[TVP] 🎯 Agent切换声明:\n"
!             f"├─ 工作流: {workflow_name}\n"
!             f"├─ 执行ID: {execution_id}\n"
!             f"├─ 目标Agent: @{stage.agent_id}\n"
!             f"├─ 调用Skill: {stage.skill_name}\n"
!             f"├─ 阶段类型: {stage.stage_type.value}\n"
!             f"└─ 工作状态: executing"
!         )
!         logger.info(tvp_msg)
  
!         if self._event_bus:
!             try:
!                 from core.deepseek_driver import TianjiEvent, EventType
!                 self._event_bus.publish(TianjiEvent(
!                     event_type=EventType.CONVERSATION_COMPLETE,
!                     source="workflow_engine",
!                     payload={
!                         "tvp_declaration": True,
!                         "workflow": workflow_name,
!                         "execution_id": execution_id,
!                         "target_agent": stage.agent_id,
!                         "skill": stage.skill_name,
!                     },
!                 ))
!             except Exception:
!                 pass
  
>     def _archive_execution(self, execution: WorkflowExecution):
>         """归档工作流执行记录到天机L3+L4"""
!         if self._bridge:
!             content = json.dumps({
!                 "type": "workflow_execution",
!                 "execution_id": execution.execution_id,
!                 "workflow": execution.workflow_name,
!                 "status": execution.status.value,
!                 "stages_completed": len([s for s in execution.stages if s.status == StageStatus.SUCCESS]),
!                 "stages_total": len(execution.stages),
!                 "total_duration_ms": execution.total_duration_ms,
!             }, ensure_ascii=False)
  
!             self._bridge.memory_post(
!                 content=content,
!                 layer="episodic",
!                 tags=["workflow", execution.workflow_name, "execution", "auto_archive"],
!                 priority="high" if execution.status == WorkflowStatus.COMPLETED else "medium",
!             )
  
!             if execution.status == WorkflowStatus.COMPLETED:
!                 self._bridge.memory_post(
!                     content=content,
!                     layer="semantic",
!                     tags=["workflow", execution.workflow_name, "pattern", "auto_archive"],
!                     priority="high",
!                 )
  
>     def resume(self, execution_id: str) -> Optional[WorkflowExecution]:
>         """从失败阶段恢复执行"""
!         execution = self._executions.get(execution_id)
!         if not execution:
!             return None
  
!         failed_stage = None
!         for stage_result in execution.stages:
!             if stage_result.status == StageStatus.FAILED:
!                 failed_stage = stage_result.stage_name
!                 break
  
!         if not failed_stage:
!             return None
  
!         wf = self._workflows.get(execution.workflow_name)
!         if not wf:
!             return None
  
!         return self.execute(
!             execution.workflow_name,
!             context=execution.context,
!             resume_from=failed_stage,
!         )
  
>     def cancel(self, execution_id: str) -> bool:
!         execution = self._executions.get(execution_id)
!         if execution and execution.status == WorkflowStatus.RUNNING:
!             execution.status = WorkflowStatus.CANCELLED
!             return True
!         return False
  
>     def get_workflow(self, name: str) -> Optional[WorkflowDefinition]:
!         return self._workflows.get(name)
  
>     def list_workflows(self) -> List[WorkflowDefinition]:
!         return list(self._workflows.values())
  
>     def get_execution(self, execution_id: str) -> Optional[WorkflowExecution]:
!         return self._executions.get(execution_id)
  
>     def get_stats(self) -> Dict:
!         return {
!             **self._stats,
!             "registered_workflows": len(self._workflows),
!             "active_executions": len([
!                 e for e in self._executions.values()
!                 if e.status == WorkflowStatus.RUNNING
!             ]),
!             "workflow_names": list(self._workflows.keys()),
!         }
  
>     def get_hermes_comparison(self) -> Dict:
!         return {
!             "hermes_resolve_toolset": {
!                 "description": "递归展开组合式工具集",
!                 "tianji_status": "✅ 已实现(增强版)",
!                 "tianji_class": "WorkflowEngine.define_from_composition()",
!                 "tianji_enhancement": "条件分支+并行阶段+错误恢复+TVP声明",
!                 "parity": "150%",
!             },
!             "hermes_toolset_composition": {
!                 "description": "Toolset = [tool_a, includes: [toolset_b]]",
!                 "tianji_status": "✅ 已实现",
!                 "tianji_class": "WorkflowStage + parallel_group",
!                 "tianji_enhancement": "3种阶段类型(顺序/并行/条件)",
!                 "parity": "120%",
!             },
!             "hermes_workflow_persistence": {
!                 "description": "工作流执行记录持久化",
!                 "tianji_status": "✅ 已实现(增强版)",
!                 "tianji_class": "WorkflowEngine._archive_execution()",
!                 "tianji_enhancement": "L3+L4双层归档+TVP声明追踪",
!                 "parity": "130%",
!             },
!             "tianji_exclusive_error_recovery": {
!                 "description": "工作流错误恢复(重试/降级/跳过)",
!                 "tianji_status": "✅ 天机独有",
!                 "parity": "N/A (Hermes无此能力)",
!             },
!             "tianji_exclusive_tvp_workflow": {
!                 "description": "工作流内Agent切换TVP声明",
!                 "tianji_status": "✅ 天机独有",
!                 "parity": "N/A (Hermes无此能力)",
!             },
!         }
