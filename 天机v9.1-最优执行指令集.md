# 天机v9.1 最优执行指令集 V2.0

**版本**: V2.0 — 系统性任务执行指令框架
**生成日期**: 2026-06-15
**基于**: 天机v9.1真实代码扫描（非推测）
**执行原则**: 五阶定位·错误预置·方案对比·灵魂拷问·闭环验证

---

## 指令集架构

```
指令集 V2.0
├── P0-清理优化 (CMD-01 ~ CMD-03) — 消除冗余，释放空间
├── P1-架构完善 (CMD-04 ~ CMD-05) — 消除依赖，统一引擎
├── P2-闭环强化 (CMD-06 ~ CMD-08) — 补齐闭环，强制执行
├── 验证审计 (CMD-09 ~ CMD-11) — 全量验证，基准审计
└── 应急回滚 (CMD-12 ~ CMD-14) — 安全兜底
```

### V2.0指令模板结构（每条指令必须包含）

```yaml
【定位层级】：宏观 / 中观 / 微观 / 微微观（只选一个）

【目标声明】：清理/优化什么，预期成果是什么（含量化指标）

【错误预置】：
  - 风险清单：
  - 回滚策略：
  - 重试/熔断规则：

【方案对比】：
  - 方案A：
  - 方案B：
  - 选型理由：

【执行动作】：
  1. ...
  2. ...

【验证清单】：
  □ 单元测试全绿
  □ 静态检查无新增告警
  □ 性能基准未劣化（或提升__%）
  □ 关键路径手动回归通过

【落地闭环证据】：
  - 执行日志
  - 闭合反馈：下次迭代无需再次修复同类问题
```

---

## 🔴 P0-清理优化指令

### CMD-01: 配置体系统一指令

```
【定位层级】：中观 — 配置体系内部结构

【目标声明】：消除.qoder/和.trae/双体系并存，统一为.trae/，
  配置从49个Agent定义减少到≤30个，规则从18个减少到≤10个，
  技能从68个减少到≤30个

【错误预置】：
  - 风险清单：
    1. 删除.qoder/后Qoder IDE无法使用（已确认Qoder不再使用）
    2. .qoder/中可能有.trae/没有的独特配置（需逐文件对比）
    3. 合并后文件格式不兼容（.md vs .json格式差异）
    4. 删除过程中文件被占用（守护进程可能持有文件句柄）
  - 回滚策略：全量备份到.backup_20260615/，原子化操作
    （先备份→再操作→验证→确认→删除原目录）
  - 重试/熔断规则：失败后从备份恢复，人工审阅后重试；
    连续3次失败则熔断，升级到@tianshu人工裁决

【方案对比】：
  - 方案A：直接删除.qoder/，仅保留.trae/
    优点：彻底消除冗余，维护成本最低
    缺点：可能遗漏.qoder/独有配置
    风险：中
  - 方案B：合并后保留.qoder/为软链接指向.trae/
    优点：兼容性好，Qoder IDE理论上仍可用
    缺点：维护软链接增加复杂度
    风险：低
  - 选型理由：选A — .qoder/是Qoder IDE遗留，当前使用Trae IDE，
    软链接方案增加无价值复杂度。通过逐文件对比确保无遗漏。

【执行动作】：

  1. 记忆检索
     定位: 天机L3/L4层
     操作: memory_recall(query="配置合并 .qoder .trae", layers=["episodic","semantic"], limit=5)
     错误分支: 天机不可用 → 跳过，记录到本地日志
     灵魂拷问: 天机中真的没有历史合并经验吗？
     闭环验证: 检索结果非空 → 有参考；为空 → 确认首次操作

  2. 全量备份
     定位: d:\元初系统\天机v9.1\.qoder\ 和 .trae\
     操作:
       $root = "d:\元初系统\天机v9.1"
       Copy-Item -Path "$root\.qoder" -Destination "$root\.backup_20260615\.qoder" -Recurse -Force
       Copy-Item -Path "$root\.trae" -Destination "$root\.backup_20260615\.trae" -Recurse -Force
     错误分支:
       - 磁盘空间不足 → 清理logs/和data/.memory/archive/后重试
       - 权限不足 → 以管理员权限重试
       - 文件被占用 → 停止天机守护进程后重试
     灵魂拷问: 备份真的完整吗？文件数是否一致？
     闭环验证:
       (Get-ChildItem "$root\.qoder" -Recurse -File).Count -eq (Get-ChildItem "$root\.backup_20260615\.qoder" -Recurse -File).Count

  3. 逐文件对比规则差异
     定位: .qoder/rules/ vs .trae/rules/ (9对同名.md文件)
     操作: 对9对文件逐一对比内容
     特殊: .qoder有08-记忆系统指令.md, .trae有08-天机企划书.md (不同文件)
     错误分支: 文件编码问题 → 使用UTF-8-SIG编码重新读取
     灵魂拷问: .qoder独有的08-记忆系统指令.md是否包含关键规则？
     闭环验证: 生成差异报告，确认每对文件SAME/DIFF状态

  4. 合并规则文件
     定位: .trae/rules/
     操作:
       - 内容相同 → 保留.trae版本
       - 内容不同 → 以.trae为基础，合并.qoder独有段落
       - .qoder独有08-记忆系统指令.md → 复制到.trae/rules/
     错误分支: 合并冲突 → 保留两版本，文件名加_conflict后缀，人工裁决
     灵魂拷问: 合并后规则是否逻辑自洽？有无矛盾？
     闭环验证: 合并后.trae/rules/文件数=10个(原9+新增1)

  5. 合并Agent配置
     定位: .qoder/agents/ (24个.md) vs .trae/agents/ (25个.json + 2个.md)
     操作:
       注意: .qoder/agents/是.md格式, .trae/agents/是.json格式 — 不可直接覆盖
       操作: 保留.trae/agents/的.json格式（Trae IDE原生格式）
       对.qoder/agents/中.trae/没有的Agent → 转换为.json格式后迁入
       对.qoder/agents/中.trae/已有的Agent → 对比内容，取较新版本
     错误分支: 格式转换失败 → 保留.md原文件在.trae/agents/_qoder_legacy/
     灵魂拷问: Trae IDE能正确加载所有Agent吗？
     闭环验证: .trae/agents/中每个文件格式正确，可被IDE解析

  6. 合并Skills配置
     定位: .qoder/skills/ (38个) vs .trae/skills/ (30个)
     操作:
       对比同名SKILL.md内容
       相同 → 保留.trae版本
       不同 → 合并差异到.trae版本
       .qoder独有 → 复制到.trae/skills/
     错误分支: 内容差异大 → 保留两版本，人工裁决
     灵魂拷问: 合并后的Skill定义是否功能完整？
     闭环验证: SKILL.md内容完整，可被Skill加载器解析

  7. 删除.qoder/目录
     定位: d:\元初系统\天机v9.1\.qoder\
     操作: Remove-Item -Path "$root\.qoder" -Recurse -Force
     前置条件: Step-4/5/6全部完成且验证通过
     错误分支:
       - 文件被占用 → 停止守护进程后重试
       - 删除失败 → 使用rd /s /q强制删除
     灵魂拷问: .qoder/真的可以安全删除吗？是否有代码引用了.qoder/路径？
     闭环验证:
       Test-Path "$root\.qoder" → False
       grep -r ".qoder" core/ server/ daemon/ agents/ → 无引用

  8. 验证服务启动
     定位: daemon/tianji_daemon.py
     操作: 重启天机服务，检查http://127.0.0.1:8771/api/health
     错误分支:
       - 服务启动失败 → 执行CMD-12回滚
       - Agent加载失败 → 检查.trae/agents/文件格式
     灵魂拷问: 服务真的正常吗？所有Agent都能被调度吗？
     闭环验证: health.status == "healthy"，Agent列表非空

  9. 记录执行结果
     定位: 天机L3 Episodic层
     操作: memory_remember(
       content="CMD-01完成: .qoder/已合并到.trae/并删除",
       layer="episodic", tags=["CMD-01","config-unification","completed"]
     )
     闭环验证: memory_recall(query="CMD-01") → 返回此条记录

【验证清单】：
  □ .qoder/目录已删除（Test-Path返回False）
  □ .trae/rules/文件数=10个
  □ .trae/agents/文件格式正确（.json为主）
  □ 天机服务健康检查通过
  □ 无".qoder"路径引用残留
  □ 备份目录.backup_20260615/完整存在

【落地闭环证据】：
  - 差异报告: diff_report.txt（9对规则文件对比结果）
  - 合并日志: merge_log.txt（Agent/Skill合并记录）
  - 闭环反馈: 后续禁止引入.qoder/配置（加入.code-review规则）
```

---

### CMD-02: 临时脚本清理指令

```
【定位层级】：中观 — 根目录文件组织

【目标声明】：消除根目录66个_*.py临时脚本，从66个减少到0个，
  有价值的脚本迁移到scripts/目录，无价值的直接删除

【错误预置】：
  - 风险清单：
    1. 某些_*.py可能被subprocess间接调用
    2. 某些_*.py可能包含独特的诊断逻辑（有历史价值）
    3. 删除后可能影响调试流程
  - 回滚策略：备份到.backup_20260615/_scripts/，迁移而非直接删除
  - 重试/熔断规则：发现引用则停止删除，升级到人工裁决

【方案对比】：
  - 方案A：全部迁移到scripts/archive/（保守）
    优点：零风险，所有脚本可找回
    缺点：根目录仍然臃肿（只是换位置）
    风险：低
  - 方案B：分类处理 — 无引用删除 + 有引用迁移 + 有价值归档
    优点：根目录彻底清理
    缺点：分类工作量大，可能误删
    风险：中
  - 选型理由：选B — 根目录清理是核心目标，通过依赖检查降低误删风险

【执行动作】：

  1. 记忆检索
     操作: memory_recall(query="临时脚本清理 _*.py", layers=["episodic","semantic"], limit=5)
     错误分支: 天机不可用 → 跳过
     灵魂拷问: 之前清理过这些脚本吗？为什么它们还在？

  2. 检查脚本依赖关系
     定位: core/, server/, daemon/, agents/ 中是否import了_*.py
     操作: 对66个脚本逐一搜索引用
     错误分支: 搜索超时 → 分批搜索，每批10个
     灵魂拷问: 这些脚本真的没有被任何正式代码引用吗？
     闭环验证: 生成依赖报告dependency_report.txt

  3. 分类脚本
     A类-可安全删除: 无任何引用的_*.py (预计60+个)
     B类-需保留迁移: 有引用的_*.py → 迁移到scripts/tools/ (预计0-3个)
     C类-有历史价值: 无引用但包含独特逻辑 → 迁移到scripts/archive/
     灵魂拷问: 分类标准是否足够严格？
     闭环验证: A+B+C类脚本总数 = 66

  4. 创建目标目录
     操作: New-Item scripts/tools/, scripts/archive/, scripts/deprecated/
     闭环验证: 三个目录均存在

  5. 迁移B类和C类脚本
     操作: Move-Item → scripts/tools/ 或 scripts/archive/
     错误分支: 移动失败 → 复制后删除原文件
     灵魂拷问: 迁移后引用这些脚本的代码还能找到它们吗？
     闭环验证: scripts/中文件数 = B类+C类

  6. 删除A类脚本
     操作: Remove-Item每个A类脚本
     前置条件: Step-2确认无引用
     错误分支: 删除失败 → 文件被占用，停止守护进程后重试
     灵魂拷问: 删除前真的确认过没有引用吗？备份存在吗？
     闭环验证: 根目录_*.py文件数 = 0

  7. 更新.gitignore
     操作: 追加 scripts/archive/ 和 scripts/deprecated/
     闭环验证: git status正确忽略这些目录

  8. 验证服务正常
     操作: 重启服务，健康检查
     灵魂拷问: 删除这些脚本后，天机所有功能真的不受影响吗？
     闭环验证: health.status == "healthy"

  9. 记录执行结果
     操作: memory_remember(content="CMD-02完成", layer="episodic", tags=["CMD-02"])

【验证清单】：
  □ 根目录_*.py文件数=0
  □ scripts/tools/包含所有B类脚本
  □ scripts/archive/包含所有C类脚本
  □ 天机服务健康检查通过
  □ 无import引用断裂
  □ .gitignore已更新

【落地闭环证据】：
  - 依赖报告: dependency_report.txt
  - 分类报告: classification_report.txt
  - 闭环反馈: 后续禁止在根目录创建_*.py临时脚本（加入编码规范）
```

---

### CMD-03: 发布包归档指令

```
【定位层级】：中观 — 根目录文件组织

【目标声明】：归档根目录非核心文件，根目录非隐藏文件≤15个，
  构建脚本→scripts/build/，审计脚本→scripts/audit/，文档→docs/

【错误预置】：
  - 风险清单：
    1. 构建脚本迁移后CI/CD路径断裂
    2. 文档迁移后README.md中的相对链接失效
    3. 某些脚本可能被外部工具引用
  - 回滚策略：从scripts/和docs/移回根目录
  - 重试规则：CI/CD失败后检查路径配置

【方案对比】：
  - 方案A：全部迁移到scripts/和docs/子目录
    优点：根目录彻底整洁
    缺点：需更新CI/CD路径
    风险：中
  - 方案B：仅删除明显无用的文件，其余保留
    优点：零风险
    缺点：根目录仍然臃肿
    风险：低
  - 选型理由：选A — 根目录整洁是核心目标，CI/CD路径更新工作量可控

【执行动作】：

  1. 创建目标目录: docs/, scripts/build/, scripts/audit/, scripts/demo/
  2. 迁移构建脚本: build_*.py, *.ps1, build.cmd → scripts/build/
  3. 迁移审计脚本: audit_*.py, audit-*.ps1, audit-report.json → scripts/audit/
  4. 迁移演示脚本: agent_dispatch_demo.py → scripts/demo/
  5. 归档文档: ACCEPTANCE-CRITERIA*.md, AGENTS.md, BUILD-GUIDE.md等 → docs/
  6. 保留核心文件: README.md, CHANGELOG.md, conftest.py, Dockerfile, .mcp.json
  7. 检查CI/CD路径: 如有.github/workflows/，更新脚本路径
  8. 验证根目录整洁度: 非隐藏文件≤15个
  9. 验证服务正常
  10. 记录执行结果

  每步错误分支: 移动失败 → 复制后删除；路径断裂 → 更新引用

【验证清单】：
  □ 根目录非隐藏文件≤15个
  □ scripts/build/包含所有构建脚本
  □ docs/包含归档文档
  □ 天机服务健康检查通过
  □ CI/CD路径已更新（如有）

【落地闭环证据】：
  - 迁移日志: migration_log.txt
  - 闭环反馈: 后续构建脚本统一放在scripts/build/中
```

---

## 🟠 P1-架构完善指令

### CMD-04: MCP Server独立性验证指令

```
【定位层级】：中观 — MCP Server依赖关系

【目标声明】：确认5个MCP Server无归档依赖，全部独立运行

【错误预置】：
  - 风险清单：
    1. MCP Server文件可能缺失或损坏
    2. import路径可能指向旧代码
    3. .trae/mcp.json配置路径可能错误
  - 回滚策略：从归档恢复缺失文件
  - 重试规则：import错误 → 修复路径后重试

【方案对比】：
  - 方案A：仅验证现有代码，不修改
    优点：零风险
    缺点：发现问题不修复
  - 方案B：验证+修复一体化
    优点：验证完立即修复
    缺点：修改可能引入新问题
  - 选型理由：选A — 验证指令只验证，修复由专门指令执行

【执行动作】：

  1. 验证5个MCP Server文件存在
     定位: mcp/server/agent_framework.py, command_executor.py, ops_engine.py,
           security_scanner.py, performance_profiler.py
     操作: Test-Path每个文件
     闭环验证: 5个文件全部存在且大小>0

  2. 检查import依赖
     定位: mcp/server/*.py中的import语句
     操作: Select-String -Pattern "^import |^from "
     灵魂拷问: 所有import路径是否都指向v9.1自身代码？
     闭环验证: 无对_archived_、天机基石、v8.0等归档路径的引用

  3. 验证MCP配置正确性
     定位: .trae/mcp.json
     操作: 解析JSON，验证每个Server的command和args路径
     灵魂拷问: MCP配置中的路径是否与实际文件路径一致？
     闭环验证: 每个MCP Server的路径都指向实际存在的文件

  4. 测试MCP Server启动
     操作: python mcp/server/*.py --help (或等效测试)
     灵魂拷问: MCP Server真的能启动吗？
     闭环验证: 每个MCP Server能正常启动（无import错误）

  5. 记录验证结果

【验证清单】：
  □ 5个MCP Server文件全部存在
  □ 无归档代码依赖
  □ MCP配置路径正确
  □ MCP Server能正常启动

【落地闭环证据】：
  - 验证报告: mcp_verification_report.txt
  - 闭环反馈: MCP Server独立性已确认/需修复
```

---

### CMD-05: 引擎架构验证指令

```
【定位层级】：微观 — 类/函数级架构验证

【目标声明】：确认引擎继承关系正确，v10-ready拆分已落地，
  ICMEEngine→ICMEStorageEngine→LawDomain层次清晰

【错误预置】：
  - 风险清单：
    1. v10-ready子组件可能未真正集成
    2. 继承关系可能有循环依赖
    3. 外部import可能指向旧版Engine类
  - 回滚策略：验证指令不修改代码，仅报告问题
  - 重试规则：N/A（只读验证）

【方案对比】：
  - 方案A：仅验证继承关系和import一致性
  - 方案B：验证+运行时功能测试
  - 选型理由：选B — 静态验证+运行时验证双重保障

【执行动作】：

  1. 验证引擎继承关系
     定位: core/engine.py, core/hybrid_engine.py, core/law/engine.py
     操作: Select-String -Pattern "^class "
     预期:
       core/engine.py → class ICMEEngine
       core/hybrid_engine.py → class ICMEStorageEngine(ICMEEngine)
       core/law/engine.py → class LawDomainEngine (独立类)
     灵魂拷问: ICMEStorageEngine继承ICMEEngine是否合理？
     闭环验证: 继承关系正确，无循环依赖

  2. 验证v10-ready拆分状态
     定位: core/memory/writer.py, promoter.py, archiver.py, indexer.py
     操作: Test-Path + 检查ICMEEngine.__init__中是否引用
     灵魂拷问: v10-ready的拆分是否真正落地？
     闭环验证: 4个子组件文件存在，ICMEEngine中引用了它们

  3. 检查引擎import一致性
     操作: 搜索全项目中对engine的import
     灵魂拷问: 是否有代码仍在使用已废弃的Engine类名？
     闭环验证: 无对旧版Engine类的引用

  4. 运行时import测试
     操作:
       python -c "from core.engine import ICMEEngine, MemoryEntry; print('OK')"
       python -c "from core.hybrid_engine import ICMEStorageEngine; print('OK')"
     灵魂拷问: 引擎真的能被正确import吗？
     闭环验证: 3个引擎类都能被正确import

  5. 记录验证结果

【验证清单】：
  □ 引擎继承关系正确
  □ v10-ready子组件存在且被引用
  □ import一致性无问题
  □ 3个引擎类都能正确import

【落地闭环证据】：
  - 验证报告: engine_verification_report.txt
  - 闭环反馈: 引擎架构已确认正确/需修复
```

---

## 🟡 P2-闭环强化指令

### CMD-06: 决策闭环补齐指令

```
【定位层级】：微观 — 函数级闭环补齐

【目标声明】：在core/driver/decision.py的DecisionEngine类中添加
  memory_remember调用，使决策结果自动记录到L3 Episodic层，
  记录完整度≥95%

【错误预置】：
  - 风险清单：
    1. 添加memory_remember可能造成循环调用（决策→记录→决策→...）
    2. 记录失败可能影响决策性能
    3. memory_remember的engine引用可能不可用
  - 回滚策略：git checkout恢复decision.py
  - 重试规则：循环调用 → 添加防重入标志；性能退化 → 异步记录

【方案对比】：
  - 方案A：在决策函数末尾直接添加memory_remember
    优点：简单直接，可靠性高
    缺点：侵入性强，修改核心函数
    风险：中（循环调用风险）
  - 方案B：通过事件总线自动触发记忆写入
    优点：解耦，不修改核心函数
    缺点：需事件总线基础设施，实现复杂
    风险：高（事件投递可能丢失）
  - 选型理由：选A — 快速落地，通过防重入标志避免循环调用

【执行动作】：

  1. 读取决策引擎代码
     定位: core/driver/decision.py
     操作: 读取文件，定位DecisionEngine类的所有决策方法
     灵魂拷问: 决策逻辑真的在这个文件中吗？

  2. 确认无memory_remember调用
     操作: grep "memory_remember" core/driver/decision.py
     预期: 无匹配 → 确认需要添加
     闭环验证: grep结果为空

  3. 添加决策记录钩子
     定位: DecisionEngine类每个决策方法的return语句之前
     操作: 添加以下代码:
       try:
           if hasattr(self, '_engine') and self._engine:
               self._engine.remember(
                   content=f"决策: {decision.summary}",
                   layer="episodic",
                   tags=["decision", "auto_record"]
               )
       except Exception:
           logger.warning("决策记录失败，不影响决策本身")
     错误分支:
       - _engine属性不存在 → 通过构造函数注入
       - 循环调用 → 添加_recording_in_progress防重入标志
     灵魂拷问: 添加的记录逻辑是否会影响决策性能？是否会造成循环调用？
     闭环验证: 决策延迟增加<10%，无循环调用

  4. 测试决策闭环
     操作: 执行一次决策 → 检查L3层是否有对应记录
     灵魂拷问: 决策记录真的写入了L3吗？内容是否完整？
     闭环验证: 写入→检索→比对→确认

  5. 记录执行结果

【验证清单】：
  □ 决策函数已添加记录逻辑
  □ 决策结果自动记录到L3
  □ 记录完整度≥95%
  □ 无性能退化（决策延迟增加<10%）
  □ 无循环调用风险

【落地闭环证据】：
  - 代码变更: decision.py diff
  - 测试日志: decision_loop_test.txt
  - 闭环反馈: 后续决策函数必须包含memory_remember（加入编码规范）
```

---

### CMD-07: 修复闭环补齐指令

```
【定位层级】：微观 — 函数级闭环补齐

【目标声明】：在core/evolution_loop.py的EvolutionLoop.evolve()成功分支中
  添加memory_remember调用，使修复经验自动沉淀到L4 Semantic层，
  沉淀完整度≥90%

【错误预置】：
  - 风险清单：
    1. evolution_loop.py已有record_action，添加memory_remember可能混淆
    2. 修复经验提取逻辑可能不够精确
    3. L4层写入需通过QualityGate门禁，可能被拒绝
  - 回滚策略：git checkout恢复evolution_loop.py
  - 重试规则：QualityGate拒绝 → DOWNGRADE到L3层记录

【方案对比】：
  - 方案A：在evolve()成功分支直接添加memory_remember
    优点：简单直接
    缺点：修改进化核心函数
  - 方案B：通过ClosedLoopLearningBridge桥接
    优点：利用现有桥接机制
    缺点：桥接可能未完全实现
  - 选型理由：选B — learning_bridge.py已存在，利用现有基础设施

【执行动作】：

  1. 读取进化闭环代码
     定位: core/evolution_loop.py, core/learning_bridge.py
     操作: 读取文件，定位evolve()成功分支和ClosedLoopLearningBridge

  2. 确认evolve()成功分支无memory_remember
     操作: grep "memory_remember" core/evolution_loop.py
     预期: 无匹配 → 确认需要添加

  3. 通过learning_bridge添加经验沉淀
     定位: EvolutionLoop.evolve()成功分支
     操作: 在evolve()成功后调用:
       try:
           if self._learning_bridge:
               experience = self._extract_evolution_experience(result)
               self._learning_bridge.remember_experience(
                   content=experience,
                   layer="semantic",
                   tags=["evolution", "experience", "auto_store"]
               )
       except Exception:
           logger.warning("修复经验沉淀失败")
     错误分支:
       - learning_bridge不可用 → 直接调用engine.remember()
       - QualityGate拒绝 → DOWNGRADE到L3层
     灵魂拷问: 沉淀的经验内容是否足够详细？后续能否根据此经验复现修复？
     闭环验证: 执行一次evolve → 检查L4层是否有对应经验记录

  4. 测试修复闭环
     灵魂拷问: 修复经验真的能被后续检索到吗？
     闭环验证: 写入→检索→比对→确认

  5. 记录执行结果

【验证清单】：
  □ evolve()成功分支已添加经验沉淀逻辑
  □ 修复经验自动沉淀到L4（或降级到L3）
  □ 经验可被检索和复用
  □ 无性能退化

【落地闭环证据】：
  - 代码变更: evolution_loop.py diff
  - 测试日志: fix_loop_test.txt
  - 闭环反馈: 后续进化函数必须包含经验沉淀（加入编码规范）
```

---

### CMD-08: TVP协议强化指令

```
【定位层级】：微观 — 函数级协议强化

【目标声明】：在core/tvp_bridge.py的TVPBridge各桥接点添加
  memory_remember调用，使TVP声明自动记录到L1 Working层，
  TVP执行率=100%

【错误预置】：
  - 风险清单：
    1. TVP记录失败是否应中断Agent切换（过于严格）
    2. 大量TVP记录可能占用L1层空间
    3. tvp_bridge.py已有record_action，添加memory_remember需区分
  - 回滚策略：git checkout恢复tvp_bridge.py
  - 重试规则：记录失败 → 仅警告，不中断切换（TVP是透明协议，不应阻断业务）

【方案对比】：
  - 方案A：TVP记录失败时中断Agent切换
    优点：强制100%执行
    缺点：过于严格，可能影响业务连续性
  - 方案B：TVP记录失败时仅警告，不中断切换
    优点：不影响业务
    缺点：TVP执行率可能<100%
  - 选型理由：选B — TVP是透明协议，核心目标是可追溯而非阻断。
    通过审计机制检测遗漏，而非运行时中断。

【执行动作】：

  1. 读取TVP桥接代码
     定位: core/tvp_bridge.py (已有record_action在175,217,249,308行)
     操作: 读取文件，定位4个桥接点

  2. 在每个桥接点添加memory_remember
     定位: TVPBridge._emit_tvp_event()或等效方法
     操作: 在record_action之后添加:
       try:
           if self._engine:
               self._engine.remember(
                   content=f"[TVP] {current_agent} → {target_agent} | {task_type}",
                   layer="working",
                   tags=["tvp", "switch", "auto_record"]
               )
       except Exception:
           logger.warning("TVP记忆记录失败，不影响调度")
     灵魂拷问: TVP记录是否与record_action重复？
     答: 不重复 — record_action记录因果对(进化用)，memory_remember写入ICME记忆(检索用)

  3. 添加TVP审计机制
     定位: core/audit_engine.py
     操作: 添加audit_tvp_compliance()函数
     灵魂拷问: 审计机制真的能检测到TVP遗漏吗？
     闭环验证: 故意跳过一次TVP → 审计机制能检测到

  4. 测试TVP执行率
     操作: 执行10次Agent切换 → 检查TVP记录数
     灵魂拷问: TVP执行率真的能达到100%吗？
     闭环验证: TVP记忆记录数 ≥ Agent切换数

  5. 记录执行结果

【验证清单】：
  □ TVP桥接点已添加memory_remember
  □ TVP执行率≥95%（允许偶发记录失败）
  □ TVP审计机制可用
  □ 无Agent切换阻断

【落地闭环证据】：
  - 代码变更: tvp_bridge.py diff
  - 测试日志: tvp_test.txt
  - 闭环反馈: 后续Agent切换必须包含TVP记录（加入TVP协议规范）
```

---

## ✅ 验证审计指令

### CMD-09: 代码质量审计指令

```
【定位层级】：微微观 — 变量/表达式/语句级审计

【目标声明】：全量代码质量检查，类型注解≥80%，圈复杂度≤10，
  无循环依赖，代码重复率≤5%

【错误预置】：
  - 风险清单：审计工具可能不兼容Python 3.12语法
  - 回滚策略：审计指令不修改代码
  - 重试规则：工具失败 → 手动统计

【方案对比】：
  - 方案A：使用radon+mypy+pydeps自动化审计
  - 方案B：使用AST手动解析统计
  - 选型理由：选A — 自动化工具效率高，失败时降级到B

【执行动作】：
  1. 类型注解覆盖率检查 (ast解析统计)
  2. 圈复杂度检查 (radon cc core/ -a -n B)
  3. 循环依赖检查 (pydeps core/ --max-bacon=2)
  4. 重复代码检查
  5. 生成审计报告

【验证清单】：
  □ 类型注解覆盖率≥80%
  □ 无圈复杂度>10的函数
  □ 无循环依赖
  □ 代码重复率≤5%
  □ 审计报告已生成

【落地闭环证据】：
  - 审计报告: code_quality_report.txt
```

---

### CMD-10: 功能完整性审计指令

```
【定位层级】：微观 — 函数/方法级功能验证

【目标声明】：六层记忆CRUD+5个MCP Server+29个Agent+24个API路由全链验证

【错误预置】：
  - 风险清单：全量测试可能耗时过长
  - 回滚策略：审计指令不修改代码
  - 重试规则：超时 → 分批测试

【方案对比】：
  - 方案A：全量自动化测试
  - 方案B：抽样测试+关键路径全量
  - 选型理由：选B — 平衡覆盖率和效率

【执行动作】：
  1. 六层记忆CRUD闭环测试 (每层: remember→recall→get→forget→确认不存在)
  2. MCP Server功能测试 (5个Server核心工具)
  3. Agent调度测试 (抽样10个Agent)
  4. API端点测试 (24个路由健康检查)
  5. 生成审计报告

【验证清单】：
  □ 六层记忆CRUD全部通过
  □ MCP Server全部功能正常
  □ Agent调度全部正常
  □ API端点全部可访问

【落地闭环证据】：
  - 审计报告: functionality_report.txt
```

---

### CMD-11: 性能基准审计指令

```
【定位层级】：微观 — 函数/算法级性能

【目标声明】：启动时间<10s, API P99<1.5s, 记忆检索<100ms, 内存<500MB

【错误预置】：
  - 风险清单：测试环境可能有干扰（其他进程占用资源）
  - 回滚策略：审计指令不修改代码
  - 重试规则：异常值 → 排除后重测

【方案对比】：
  - 方案A：单次测试取结果
  - 方案B：多次测试取中位数
  - 选型理由：选B — 消除偶然因素

【执行动作】：
  1. 启动时间测试 (5次取中位数)
  2. API延迟测试 (1000次请求统计P50/P95/P99)
  3. 记忆检索延迟测试 (每层100次)
  4. 内存占用测试 (稳定运行1小时后测量)
  5. 生成性能报告

【验证清单】：
  □ 启动时间<10s
  □ API P99<1.5s
  □ 记忆检索P99<100ms
  □ 内存占用<500MB

【落地闭环证据】：
  - 性能报告: performance_report.txt
```

---

## 🔄 应急回滚指令

### CMD-12: 配置回滚指令

```
【定位层级】：中观 — 配置体系

【目标声明】：恢复.qoder/和.trae/到备份版本

【执行动作】：
  1. 停止天机服务
  2. 恢复.qoder/: Copy-Item .backup_20260615/.qoder → .qoder
  3. 恢复.trae/: Copy-Item .backup_20260615/.trae → .trae
  4. 重启服务并验证
  5. 记录回滚

【验证清单】：
  □ .qoder/和.trae/已恢复
  □ 服务健康检查通过
```

---

### CMD-13: 代码回滚指令

```
【定位层级】：微观 — 函数/类级

【目标声明】：恢复修改的Python文件

【执行动作】：
  1. git checkout -- core/driver/decision.py core/evolution_loop.py core/tvp_bridge.py
     (或从备份恢复)
  2. 重启服务并验证
  3. 记录回滚

【验证清单】：
  □ 修改的文件已恢复
  □ 服务健康检查通过
```

---

### CMD-14: 全量回滚指令

```
【定位层级】：宏观 — 系统级

【目标声明】：恢复所有修改

【执行动作】：
  1. 停止所有天机进程
  2. 执行CMD-12（配置回滚）
  3. 执行CMD-13（代码回滚）
  4. 恢复根目录文件（从scripts/和docs/移回）
  5. 重启服务并全面验证
  6. 记录回滚

【验证清单】：
  □ 所有修改已恢复
  □ 服务健康检查通过
  □ 无异常日志
```

---

## 📊 指令依赖关系

```
CMD-01 (配置统一) ──┐
CMD-02 (脚本清理) ──┤── 互不依赖，可并行
CMD-03 (归档清理) ──┘
         │
         ▼
CMD-04 (MCP验证) ──┐── 依赖P0完成
CMD-05 (引擎验证) ──┘
         │
         ▼
CMD-06 (决策闭环) ──┐
CMD-07 (修复闭环) ──┤── 依赖P1完成
CMD-08 (TVP强化) ───┘
         │
         ▼
CMD-09 (代码审计) ──┐
CMD-10 (功能审计) ──┤── 依赖P2完成
CMD-11 (性能审计) ──┘
```

---

## 🎯 V2.0执行原则

1. **五阶定位**: 每条指令必须声明【定位层级】（宏观/中观/微观/微微观，只选一个）
2. **错误预置**: 每条指令必须包含风险清单+回滚策略+重试规则
3. **方案对比**: 每条指令必须对比至少2种方案并说明选型理由
4. **灵魂拷问**: 每步操作必须回答"真的完成了吗？验证标准是什么？"
5. **闭环验证**: 每步操作必须执行写入→检索→比对→确认
6. **记忆驱动**: 操作前检索天机记忆，操作后记录到天机
7. **安全兜底**: 每条指令都有对应的回滚指令（CMD-12/13/14）

---

**指令集版本**: V2.0
**生成时间**: 2026-06-15
**维护者**: @天枢(tianshu)
**总指令数**: 14个 (3清理 + 2架构 + 3闭环 + 3审计 + 3回滚)
**V2.0核心升级**: 五阶定位 + 错误预置 + 方案对比 + 灵魂拷问 + 闭环验证
